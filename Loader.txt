--open source cuz literally our github cooked :(
local StarterGui = game:GetService("StarterGui")
local TweenService = game:GetService("TweenService")
local Lighting = game:GetService("Lighting")
local CoreGui = game:GetService("CoreGui")
local Players = game:GetService("Players")
local StarterGui = game:GetService("StarterGui")
local player = Players.LocalPlayer
local keyFile = "key.txt"
local CoreGui = game:GetService("CoreGui")
local VirtualInputManager = game:GetService("VirtualInputManager")

local fileName = "Atoggle.png"
local fileUrl = "https://github.com/CommonSense12/Gagi/blob/main/New%20Project%2032%20%5B974E3BC%5D.png?raw=true"

if not isfile(fileName) then
    writefile(fileName, game:HttpGet(fileUrl))
end

local gui = Instance.new("ScreenGui")
gui.IgnoreGuiInset = true
gui.ZIndexBehavior = Enum.ZIndexBehavior.Global
gui.DisplayOrder = 999999999
gui.ResetOnSpawn = false

if syn and syn.protect_gui then
    syn.protect_gui(gui)
    gui.Parent = CoreGui
else
    gui.Parent = CoreGui
end

local button = Instance.new("ImageButton")
button.Size = UDim2.new(0, 90, 0, 90)
button.Position = UDim2.new(0.5, -45, 0.5, -45)
button.BackgroundTransparency = 0
button.BackgroundColor3 = Color3.fromRGB(30,30,30)
button.Image = getcustomasset(fileName)
button.ZIndex = 999999999
button.Draggable = true
button.Active = true
button.Selectable = false
button.Parent = gui

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 18)
corner.Parent = button

local enabled = false
button.MouseButton1Click:Connect(function()
    enabled = not enabled
    if enabled then
        button.BackgroundColor3 = Color3.fromRGB(60, 200, 60)
        VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.H, false, game)
    else
        button.BackgroundColor3 = Color3.fromRGB(30,30,30)
        VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.H, false, game)
    end
end)

local function Screen()
	local blur = Instance.new("BlurEffect")
	blur.Size = 0
	blur.Parent = Lighting

	local gui = Instance.new("ScreenGui")
	gui.Name = "ArvotheonTitle"
	gui.IgnoreGuiInset = true
	gui.ResetOnSpawn = false
	gui.DisplayOrder = 999999
	gui.Parent = gethui()  -- <- Use gethui() for hidden GUI

	local darkFrame = Instance.new("Frame")
	darkFrame.BackgroundColor3 = Color3.new(0, 0, 0)
	darkFrame.BackgroundTransparency = 1
	darkFrame.Size = UDim2.new(1, 0, 1, 0)
	darkFrame.ZIndex = 0
	darkFrame.Parent = gui

	local textLabel = Instance.new("TextLabel")
	textLabel.Size = UDim2.new(0.4, 0, 0.15, 0)
	textLabel.Position = UDim2.new(0.3, 0, 0.425, 0)
	textLabel.BackgroundTransparency = 1
	textLabel.Text = "Arvotheon"
	textLabel.Font = Enum.Font.GothamBlack
	textLabel.TextScaled = true
	textLabel.TextTransparency = 1
	textLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
	textLabel.ZIndex = 2
	textLabel.Parent = gui

	local gradient = Instance.new("UIGradient")
	gradient.Color = ColorSequence.new{
		ColorSequenceKeypoint.new(0, Color3.fromRGB(0, 174, 255)),
		ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 255, 255))
	}
	gradient.Parent = textLabel

	TweenService:Create(blur, TweenInfo.new(1), {Size = 20}):Play()
	TweenService:Create(darkFrame, TweenInfo.new(1), {BackgroundTransparency = 0.3}):Play()
	task.wait(0.6)
	TweenService:Create(textLabel, TweenInfo.new(1), {TextTransparency = 0}):Play()
	task.wait(3)
	TweenService:Create(textLabel, TweenInfo.new(1), {TextTransparency = 1}):Play()
	TweenService:Create(darkFrame, TweenInfo.new(1), {BackgroundTransparency = 1}):Play()
	TweenService:Create(blur, TweenInfo.new(1), {Size = 0}):Play()
	task.delay(2, function()
		gui:Destroy()
		blur:Destroy()
	end)
end

Screen()
task.wait(3)

local StarterGui = game:GetService("StarterGui")
local function notify(title, text)
    StarterGui:SetCore("SendNotification", {
        Title = title;
        Text = text;
        Duration = 5,
    })
end
if game.PlaceId == 126509999114328 then
loadstring(game:HttpGet('https://raw.githubusercontent.com/ReviewCardex/ArvoRevisionedNew/refs/heads/main/99nights.lua'))()
elseif game.PlaceId == 109983668079237 or game.PlaceId == 96342491571673 then
loadstring(game:HttpGet("https://raw.githubusercontent.com/ReviewCardex/ArvoRevisionedNew/refs/heads/main/StealaBrain.lua"))()
elseif game.PlaceId == 79546208627805 then
notify("Error Bruh","Do not load at the lobby")
else
notify("bruh","Unsupported")
end
